package 백준_B12891_DNA비밀번호;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class 백준_B12891_DNA비밀번호 {
	
	private void check() {
		if (minA <= )
	}
	
	
	
	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		StringTokenizer st = new StringTokenizer(br.readLine());
		
		int S = Integer.parseInt(st.nextToken());
		int P = Integer.parseInt(st.nextToken());
		int minA,minC,minG,minT;
		int cntA,cntC,cntG,cntT;
		
		minA = Integer.parseInt(br.readLine());
		minC = Integer.parseInt(br.readLine());
		minG = Integer.parseInt(br.readLine());
		minT = Integer.parseInt(br.readLine());
		
		
		
		
		char [] dna = new char[S];
		dna = br.readLine().toCharArray();
		
		
		
	}
}
